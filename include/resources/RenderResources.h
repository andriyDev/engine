
#pragma once

#include "std.h"

enum class RenderResources : uint
{
    Mesh,
    RenderableMesh,
    Shader,
    MaterialProgram,
    Material,
    Texture
};

enum class FileRenderResources : uint
{
    Mesh,
    Shader,
    Texture
};
