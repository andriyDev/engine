
#pragma once

#include "std.h"

enum class RenderResources : uint
{
    Mesh,
    RenderableMesh,
    Shader,
    MaterialProgram,
    Material
};

enum class FileRenderResources : uint
{
    Mesh,
    Shader
};
